version: 1
artifact_type: operator.source_files
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, items, relations, unresolved, analysis_status, reason, sources]
item_kind_enum: [source_file, dependency_file, external_system_file, third_party_file, generated_file, excluded_file, uncertain_file, architecture_variant, include_rule, exclude_rule, branch_skip, source_hint]
required_item_fields: [id, kind, status, identity, sources]
kind_required_fields:
  source_file: [path, role, include_reason]
  dependency_file: [path, role, discovered_from, discovery_chain, dependency_type, included_because, outside_operator_directory]
  external_system_file: [logical_path, dependency_type, discovered_from]
  third_party_file: [logical_path, dependency_type, discovered_from]
  generated_file: [path, role, include_reason]
  excluded_file: [path, role, exclude_reason]
  uncertain_file: [path, reason]
  architecture_variant: [files]
relation_required_fields: [id, type, source_id, target_id, status, sources]
source_anchor: required_for_confirmed
minimum_cardinality: 1
stores: [source_files, roles, include_edges, file_hashes, include_or_exclude_reason]
forbids: [semantic_claim_without_source]


