version: 1
schema_kind: source_anchor
required_fields: [id, file, symbol, span, anchor_kind]
forbidden_fields: [source_text, code_hash, file_hash, encoding, newline, bom]
span_required: [start_line, end_line]

