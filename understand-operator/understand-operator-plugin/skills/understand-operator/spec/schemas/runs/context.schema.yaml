version: 1
schema_kind: runtime_artifact
required_top_level: [version, artifact, snapshot, project_root, op_name, script_dir, run_id, source_revision, source_snapshot_id, spec_bundle_hash]
allowed_top_level:
  - version
  - artifact
  - snapshot
  - project_root
  - op_name
  - script_dir
  - run_id
  - source_revision
  - source_snapshot_id
  - spec_bundle_hash
required_item_fields: []
relation_required_fields: []
