version: 1
artifact_type: graph.derived.indexes
required_top_level: [version, artifact, snapshot, indexes, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, indexes, items, relations, unresolved]
item_kind_enum: [derived_index_record]
required_item_fields: [id, kind]
kind_required_fields: {}
relation_required_fields: [id, type, source_id, target_id]
minimum_cardinality: 0
stores: [terminology_index, semantic_keyword_index, node_index]
forbids: [new_source_fact]

