version: 1
artifact_type: graph.derived.expansions
required_top_level: [version, artifact, snapshot, expansions, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, expansions, items, relations, unresolved]
item_kind_enum: [derived_expansion_record]
required_item_fields: [id, kind]
kind_required_fields: {}
relation_required_fields: [id, type, source_id, target_id]
minimum_cardinality: 0
stores: [derived_node_or_edge, raw_nodes, raw_edges, yaml_pointer]
forbids: [untraceable_expansion]

