version: 1
artifact_type: graph.raw.nodes
required_top_level: [version, artifact, snapshot, nodes, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, nodes, items, relations, unresolved]
item_kind_enum: [raw_node_record]
required_item_fields: [id, kind]
kind_required_fields: {}
relation_required_fields: [id, type, source_id, target_id]
minimum_cardinality: 0
stores: [id, kind, label, detail_ref, source_refs]
forbids: [full_yaml_copy]

