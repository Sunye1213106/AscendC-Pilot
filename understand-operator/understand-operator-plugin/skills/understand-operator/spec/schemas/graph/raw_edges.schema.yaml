version: 1
artifact_type: graph.raw.edges
required_top_level: [version, artifact, snapshot, edges, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, edges, items, relations, unresolved]
item_kind_enum: [raw_edge_record]
required_item_fields: [id, kind]
kind_required_fields: {}
relation_required_fields: [id, type, source_id, target_id]
minimum_cardinality: 0
stores: [source_id, target_id, relation_type, detail_ref, source_refs]
forbids: [untyped_relation]

