version: 1
artifact_type: host.expressions
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [host_expression, predicate_expression, value_expression, shape_expression]
required_item_fields: [id, kind, status, identity, sources]
kind_required_fields:
  host_expression: [expression_text, expression_kind, operator, operand_refs, result_type, scope_ref]
  predicate_expression: [expression_text, expression_kind, operator, operand_refs, result_type, scope_ref]
  value_expression: [expression_text, expression_kind, operator, operand_refs, result_type, scope_ref]
  shape_expression: [expression_text, expression_kind, operator, operand_refs, result_type, scope_ref]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [source_text, operators, operands, comparisons, logic, arithmetic, bit_ops, shape_reads, attribute_checks, derived_value_formula]
forbids: [natural_language_only]


