version: 1
artifact_type: host.tiling_key_constraints
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [value_constraint, range_constraint, requires_constraint, implies_constraint, mutex_constraint, pruning_rule, merging_rule, unreachable_combination, input_realization]
required_item_fields: [id, kind, status, identity, sources]
kind_required_fields:
  value_constraint: [condition_ref, constrained_refs, proof_fact_ref]
  range_constraint: [condition_ref, constrained_refs, proof_fact_ref]
  requires_constraint: [condition_ref, constrained_refs, proof_fact_ref]
  implies_constraint: [condition_ref, constrained_refs, proof_fact_ref]
  mutex_constraint: [condition_ref, constrained_refs, proof_fact_ref]
  pruning_rule: [condition_ref, constrained_refs, proof_fact_ref]
  merging_rule: [condition_ref, constrained_refs, proof_fact_ref]
  unreachable_combination: [condition_ref, constrained_refs, proof_fact_ref]
  input_realization: [condition_ref, constrained_refs, proof_fact_ref]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [value_constraints, range_constraints, mutex, implies, requires, compatible_set, compile_time_fixed, runtime_guard, pruning, merging, key_unreachable, input_realization]
forbids: [constraint_without_source_proof]


