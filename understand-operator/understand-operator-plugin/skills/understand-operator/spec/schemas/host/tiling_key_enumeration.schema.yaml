version: 1
artifact_type: host.tiling_key_enumeration
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [tiling_key_enumeration_block]
required_item_fields: [id, kind, status, identity, sources]
kind_required_fields:
  tiling_key_enumeration_block: [macro_or_template_ref, fixed_fields, field_domains, product_count, instantiation_refs]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 0
stores: [template_macro_blocks, fixed_fields, field_domains, product_count, tiling_struct, reverse_realization_hints, total_size]
forbids: [generated_test_rows]


