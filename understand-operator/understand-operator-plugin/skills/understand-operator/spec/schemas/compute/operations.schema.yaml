version: 1
artifact_type: compute.operations
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [compute_operation, cast_operation, reduce_operation, broadcast_operation, reshape_operation, transpose_operation, arithmetic_operation, generic_fact]
required_item_fields: [id, kind, name, status, sources]
kind_required_fields:
  compute_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  cast_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  reduce_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  broadcast_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  reshape_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  transpose_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  arithmetic_operation: [operation_type, execution_order, implementation_ref, kernel_api_refs, golden_ref, input_tensor_refs, output_tensor_refs, axis_refs, formula, dtype_policy, broadcast_policy, reduction_policy, numerical_sensitivity, accumulation_dtype, tolerance_ref]
  generic_fact: [name]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [matmul, reduce, broadcast, reshape, transpose, cast, arithmetic, mask, normalization, other_operations, formulas, operation_order, implementation_refs, kernel_api_refs, golden_refs, axis_semantics, dtype_policy, numerical_sensitivity]
forbids: [generated_golden_code]
