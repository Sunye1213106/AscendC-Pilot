version: 1
artifact_type: kernel.slice.memory
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [memory_resource, global_tensor_resource, local_tensor_resource, queue_resource, buffer_resource, workspace_resource]
required_item_fields: [id, kind, name, status, sources]
kind_required_fields:
  memory_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
  global_tensor_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
  local_tensor_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
  queue_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
  buffer_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
  workspace_resource: [resource_kind, allocation_site_ref, producer_refs, consumer_refs, lifetime_start_ref, lifetime_end_ref, reuse_refs, queue_operation_refs]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [global_tensor, local_tensor, tque, tbuf, workspace, allocation, lifetime, producer, consumer, reuse, enqueue, dequeue]
forbids: [resource_without_producer_consumer_unless_unresolved]

