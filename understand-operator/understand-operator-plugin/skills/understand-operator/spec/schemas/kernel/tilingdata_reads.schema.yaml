version: 1
artifact_type: kernel.slice.tilingdata_reads
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [tilingdata_read]
required_item_fields: [id, kind, name, status, sources]
kind_required_fields:
  tilingdata_read: [struct_ref, field_ref, field_type, read_site_ref, target_variable_ref, read_condition_ref, host_write_candidate_ref]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [tilingdata_struct, field, read_site, target_variable, field_type, read_condition, host_write_candidate]
forbids: [host_write_fabrication]

