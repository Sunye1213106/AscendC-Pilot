version: 1
artifact_type: kernel.overview.functions
required_top_level: [version, artifact, snapshot, items, relations, unresolved]
allowed_top_level: [version, artifact, snapshot, analysis_status, reason, sources, items, relations, unresolved]
item_kind_enum: [kernel_function, kernel_method, helper_function]
required_item_fields: [id, kind, status, identity, sources]
kind_required_fields:
  kernel_function: [file, symbol, function_symbol, signature, architecture_variant, template_binding]
  kernel_method: [file, symbol, function_symbol, signature, architecture_variant, template_binding]
  helper_function: [file, symbol, function_symbol, signature, architecture_variant, template_binding]
relation_required_fields: [id, type, source_id, target_id, status]
minimum_cardinality: 1
stores: [functions, definition_location, owning_class, call_role, main_function_flag, helper_flag]
forbids: [branch_semantics_without_slice]


