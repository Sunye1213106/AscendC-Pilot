# Variable Constraint Model

Build the variable model before relations and constraints.

Classify variable source, domain, assignment, derived expression, controlling role, compile-time/runtime status, template binding, TilingKey field, TilingData field, and field-to-field constraints.

Capture `requires`, `implies`, `mutex`, pruning, merging, unreachable combinations, and input realization only when source-proven.
