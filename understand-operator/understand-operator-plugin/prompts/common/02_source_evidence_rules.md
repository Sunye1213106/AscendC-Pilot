# Source Evidence Rules

Every confirmed item or relation must carry repo-relative `source.file`, `symbol`, `span`, exact `source_text`, `code_hash`, and `anchor_kind`.

Search results and filenames alone are not proof. Use source spans and CBM/source reading together when needed.

One semantic relation may have multiple anchors. If the exact source proof is missing or ambiguous, write `unresolved`.
