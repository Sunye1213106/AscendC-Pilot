# Retired Prompt

This prompt belonged to the old compute/flow canonical workflow.

In the Phase 0-3 workflow, use `agents/uo-flow-extraction.md`. The agent writes
only `facts/compute/**`, and Step 2 validation/review/receipt handles quality.
