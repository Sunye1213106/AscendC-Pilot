# Retired Prompt

The old standalone quality phase is retired.

Run `quality_gate.py` only as the final gate inside Phase 3. It checks Phase 0
receipt, Step 1 validation, Step 2/Step 3 receipt freshness, compile gate
freshness, raw graph consistency, derived graph reversibility, read-only query,
and absence of generated test results.

