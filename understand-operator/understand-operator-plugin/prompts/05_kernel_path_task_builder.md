# Retired Prompt

The old kernel path task builder is retired.

In Phase 3, use `uo-kernel-slice-planner`. It reads Host, Compute, and Kernel
Overview facts and writes only `facts/kernel/slice_manifest.yaml` and
`facts/kernel/slice_interfaces.yaml`.

