# Retired Prompt

The old route builder is retired.

Query uses `manifest.yaml`, `indexes/terminology.yaml`, `graphs/derived/**`,
`graphs/raw/**`, YAML facts, and source anchors. It does not generate route
files or contracts.
