# Retired Prompt

The old kernel path agent prompt is retired.

Use `uo-kernel-slice-agent` for Phase 3 slice facts. Slice agents write only the
fixed YAML set under `facts/kernel/slices/<slice_id>/`.

