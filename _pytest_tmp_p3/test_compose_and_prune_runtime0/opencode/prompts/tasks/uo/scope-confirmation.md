# RESERVED — scope-confirmation (deprecated)

This prompt is **not** loaded on the formal UO path.
`prepare` establishes Source Scope machine-side; do not ask humans to confirm file lists.

If you were dispatched here: treat as `SCOPE_VALIDATE_BLOCKED` / routing bug and stop.
