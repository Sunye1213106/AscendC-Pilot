# Cursor host defaults for composed skills.
# Workflow slash entries stay invokable. Action Skills are internal (compose
# also sets disable-model-invocation on the cognitive copies).
skill_defaults:
  disable-model-invocation: false
skills: {}
