# Cursor host defaults for composed skills.
# Workflow slash entries: model-invokable. Cognitive skills: internal only.
skill_defaults:
  disable-model-invocation: false
skills:
  operator-analysis:
    disable-model-invocation: true
  testcase-generation:
    disable-model-invocation: true
  source-proof:
    disable-model-invocation: true
  code-review:
    disable-model-invocation: true
