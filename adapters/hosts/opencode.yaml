# OpenCode host defaults for composed skills.
# Only Workflow Entry skills are installed into Skill discovery.
# Cognitive skills live under generated/opencode/cognitive-skills/ (plugin-internal).
skill_defaults:
  disable-model-invocation: false
skills: {}
