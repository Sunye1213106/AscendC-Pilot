# TG engine

Python package `testcase_agent` — test contract / plan / Z3 solve.

```text
pip install -e "./engines/tg[solver]"
```

Canonical product root: `<repo>/.ascendc-agent/tg/`  
UO is read-only at `<repo>/.ascendc-agent/uo/`.
