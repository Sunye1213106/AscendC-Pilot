# Code Engineering (CE)

Python package: `code_engineering`.

```text
diff → host_codemap writers/predicates → fields[] → TG regression cases
```

```powershell
pip install -e ./engines/code-engineering
ce-impact --diff pr.diff --root .
```
