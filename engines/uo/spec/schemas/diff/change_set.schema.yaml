version: 1
schema_kind: diff_product
required_top_level: [version, base_revision, head_revision, files]
allowed_top_level:
  - version
  - op_name
  - base_revision
  - head_revision
  - needs_phase0_review
  - scoped_change_count
  - files
required_item_fields: []
relation_required_fields: []
notes: |
  files[].path/status/in_scope/role are required for each entry.
  status is A|M|D (or git rename treated as new path).
