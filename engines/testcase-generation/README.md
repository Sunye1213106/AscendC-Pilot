# TG engine

Python package `testcase_agent` — test contract / plan / Z3 solve.

```text
pip install -e "./engines/testcase-generation[ml]"
```

Canonical product root: `<repo>/.ascendc-pilot/tg/`  
UO is read-only at `<repo>/.ascendc-pilot/uo/`.
