# 测试范围从哪来

**何时加载**：确定本轮测试范围从哪来、能不能派 `/tg-plan` 时。

本步定义如何把已确定的范围写成义务，不负责编排审查或查询。

合法来源：

- 同一会话审查结论
- `{slug}_plan.md` 的「测试内容」
- 用户已陈述的测试范围
- `session_handoff.md`
- 最终产物是用例、审查不是交付物时，主控综合的查询结论

缺 Planning Context 时不要进入 `/tg-plan`。最终产物是用例、审查不是交付物时，不要把审查推理成前置。只有审查本身是交付物时才用审查结论。
