```yaml
plan_tools:
  - Read repo_scan.yaml for table headers and tables[].profile value ranges
  - Open the precision entry script and locate the richest torch_npu.npu_block_matmul call
  - Draft call_args for every positional and keyword argument with source_column traces
  - Assign mapping roles from call_args only (api_arg vs feature; no attr)
  - Query operator cards within the 8-identifier budget to fill uo_id on bound columns
  - Fill domains.operator/compare and run the completion checklist before handoff
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: Last dimension when building left/right; also the Width symbol inside math.sqrt(Width) for scale=.
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed verbatim as the batch= keyword argument.
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: Segment descriptor normalized by normalize(...) into packed=; active only on the Packed layout branch.
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: Tail-pad count; real_n = Batch - pad_tail then segs is sliced to real_n before normalize(segs).
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: Flag that builds an optional mask tensor in a Python pre-step; mask is not a npu_block_matmul kwarg.
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: Dim=Packed
    compare: match
  pad_tail:
    operator: padToken
    compare: match
  gate_on:
    operator: maskOuter
    compare: match
findings:
  - scale= is 1.0 / math.sqrt(Width); no CSV column named scale (call_args source_column null for scale).
  - left and right are positional tensors built from case data with Width as the last dim; no direct CSV column names the positional args.
  - gate_on drives mask construction in a pre-step only; mask does not appear in the richest call.
  - pad_tail slices segs before packed= but is absent from the call signature.
  - segs/packed wiring applies only under Packed layout (dim_names includes Packed).
verify: Confirm call_args coverage, every api_arg has a header-backed uo_id, feature columns have no call wiring, and domains.compare values are consistent with tables[].profile when scan is available.
```
