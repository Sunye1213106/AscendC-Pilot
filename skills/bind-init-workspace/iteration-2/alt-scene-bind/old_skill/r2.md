```yaml
plan_tools:
  - Read repo_scan.yaml table headers and tables[].profile for the five columns
  - Open the precision entry script and extract the richest torch_npu.npu_block_matmul call
  - Draft call_args from positional left/right plus batch, scale, and packed kwargs before assigning roles
  - Run parameterless uo-query for dim_names Packed and DType hints
  - Query identifiers b, width, packedSeg, padToken, maskOuter within the 8-query budget
  - Edit parts/bind.yaml semantic slots only; do not invent columns or change schema/run_id

mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: Last dimension when constructing left/right; also the divisor inside math.sqrt for the scale kwarg (no Scale column).
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed directly as batch= to npu_block_matmul.
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: Segment-length vector normalized and passed as packed=; active only on the Packed layout branch.
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: Trailing pad count; script sets real_n = Batch - pad_tail and slices segs before normalize; never appears in the API call.
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: When set, script builds a mask tensor in a Python pre-step only; mask is not a kwarg of npu_block_matmul.

domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: "packedSeg; Dim=Packed"
    compare: match
  pad_tail:
    operator: padToken
    compare: tighter_operator
  gate_on:
    operator: maskOuter
    compare: tighter_operator

findings:
  - scale kwarg is the on-site formula 1.0 / math.sqrt(Width); no CSV column named scale (source_column null for scale in call_args).
  - mask is built when gate_on is set but is omitted from the npu_block_matmul call entirely.
  - pad_tail only reshapes the segs preamble (slice to real_n) and is not wired into any call argument.
  - segs/packed path is layout-gated (dim_names Packed); other modes may omit the packed kwarg while the column remains api_arg per richest call.

verify: Re-read the richest call and confirm Batch/segs/Width appear in call_args as api_arg with uo_id, pad_tail and gate_on stay feature, scale/mask gaps are in findings, and domains.operator identifiers match the tiling_data header without TDF:: ids.
```
