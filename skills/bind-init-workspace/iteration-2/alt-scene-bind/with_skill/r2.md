plan_tools:
  - Read richest precision-entry call window (torch_npu.npu_block_matmul)
  - Read case-builder / CSV-to-kwargs mapping segment only
  - Read tiling_data header once for short-name set
  - uo-query only for header/call gaps (budget ≤8)
  - Edit parts/bind.yaml semantic grid (mapping + domains)
  - pilot_cli inspect yaml --rel <draft path under .ascendc-pilot>
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: last dimension when building left/right; also the Width inside scale=1.0/math.sqrt(Width)
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed directly as batch= kwarg
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: segment descriptor normalized into packed=; branch runs only under Packed layout
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: tail-padding count; real_n=Batch-pad_tail slices segs before normalize; not in call
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: when set, runner builds a mask tensor in Python pre-step only; mask is not a call kwarg
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator: ""
    compare: ""
  gate_on:
    operator: ""
    compare: ""
findings:
  - "scale=1.0/math.sqrt(Width) is computed at call site; no CSV column named scale (test_harness_gap)"
  - "left/right tensors are built from table rows; no dedicated CSV columns for those positional args"
  - "PARTIAL: header field width1 has no corresponding CSV column"
  - "segs column appears only under Packed layout (dim_names); role/uo_id still bind to packedSeg, not Dim=Packed"
verify: pilot_cli inspect yaml --rel skills/bind-init-workspace/iteration-2/alt-scene-bind/with_skill/bind.yaml returns ok
