```yaml
plan_tools:
  - Read repo_scan.yaml tables[].profile for column value ranges
  - Open richest precision-entry call window and case-builder mapping segment
  - Read tiling_data.h once to collect short-name set (b, width, width1, scaleValue, packedSeg, maskOuter, padToken)
  - Write call_args from torch_npu.npu_block_matmul kwargs before assigning column roles
  - Freeze roles per decision tree, then map uo_id from call kwargs / header fields only
  - pilot_cli inspect yaml --rel skills/bind-init-workspace/iteration-2/alt-scene-bind/with_skill/r1.md
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: CSV last-dimension size for left/right tensors and the Width operand inside math.sqrt for scale
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV batch count passed directly as batch= kwarg
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: CSV packed-segment descriptor normalized into packed= kwarg (Packed layout branch only)
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: tail-padding count subtracted from Batch before segs is sliced; not a call kwarg
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: flag enabling optional mask tensor build in Python pre-step; mask is not a call kwarg
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator: ''
    compare: ''
  gate_on:
    operator: ''
    compare: ''
findings:
  - scale=1.0/math.sqrt(Width) is a computed kwarg with no CSV column (source_column null; test_harness_gap)
  - gate_on drives a Python-only mask pre-step; mask is absent from npu_block_matmul call args
  - pad_tail rewrites segs before normalize/packed; column stays feature per indirect-kwargs rule
verify: pilot_cli inspect yaml --rel skills/bind-init-workspace/iteration-2/alt-scene-bind/with_skill/r1.md
```
