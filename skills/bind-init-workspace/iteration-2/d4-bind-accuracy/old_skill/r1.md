plan_tools:
  - Read repo_scan.yaml table header and tables[].profile for the target CSV
  - Open the precision entry script and locate the richest torch_npu.npu_fusion_attention_grad_v2 call site
  - Trace get_case / CaseConfig variables to draft call_args before assigning any column roles
  - Run uo-query once with no parameters for dim_names and operator index
  - Run bounded uo-query by camelCase identifiers for api_arg uo_id (budget ≤8)
  - Edit parts/bind.yaml mapping and domains; confirm checklist then hand off
mapping:
  D:
    role: api_arg
    uo_id: d
    encoding: Last dim when building q/k/v tensors; also the D in scale_value=1.0/math.sqrt(D)
  prefix:
    role: api_arg
    uo_id: prefix
    encoding: CSV cell passed directly as prefix= (profile empty_rate=1.0 but column is still read)
  inner_drop:
    role: feature
    uo_id: dropMaskOuter
    encoding: When set, script builds a drop_mask tensor; drop_mask does not appear in the grad v2 call
  eod:
    role: feature
    uo_id: null
    encoding: TND-only; real_b=B-eod slices seqlens lists before normalize; not a call kwarg
  seqlens_list_q:
    role: api_arg
    uo_id: actualSeqQlen
    encoding: TND-only Q seqlen list; normalize(...) becomes actual_seq_qlen=
domains:
  D:
    operator: d
    compare: match
  prefix:
    operator: prefix
    compare: tighter_profile
  inner_drop:
    operator: dropMaskOuter
    compare: match
  eod:
    operator: null
    compare: match
  seqlens_list_q:
    operator: actualSeqQlen
    compare: match
findings:
  - scale_value=1.0/math.sqrt(D) derives from column D; no CSV column named scale_value or scale
  - inner_drop builds drop_mask but the richest grad v2 call omits drop_mask from its kwargs
  - eod and seqlens_list_q normalize/slice logic runs only under TND layout
  - prefix remains api_arg despite profile empty_rate=1.0
  - PARTIAL uo_id for eod: no header identifier matches batch-trimming eod semantics
verify: Cross-check call_args source columns against mapping roles; every api_arg has a header short-name uo_id; feature columns are not mislabeled api_arg.
