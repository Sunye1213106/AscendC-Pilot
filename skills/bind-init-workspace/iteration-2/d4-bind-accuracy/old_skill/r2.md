```yaml
plan_tools:
  - repo_scan.yaml — read tables[].profile and column headers for the case table
  - Entry script — open richest precision call site (npu_fusion_attention_grad_v2, not profiler)
  - get_case / CaseConfig — trace kwargs and tensor builders back to CSV columns; draft call_args
  - uo-query (no args) — index dim_names / hint before identifier lookups
  - uo-query identifiers — d, prefix, actualSeqQlen (and feature-adjacent b, dropMaskOuter if budget allows)
  - Edit parts/bind.yaml — write mapping + domains semantic fields; stop
mapping:
  D:
    role: api_arg
    uo_id: d
    encoding: Last q/k/v tensor dim; also the D in scale_value=1.0/math.sqrt(D).
  prefix:
    role: api_arg
    uo_id: prefix
    encoding: CSV cell passed directly as prefix= kwarg.
  inner_drop:
    role: feature
    uo_id: dropMaskOuter
    encoding: When set, script builds a local drop_mask tensor; drop_mask not passed in this call window.
  eod:
    role: feature
    uo_id: b
    encoding: TND branch only; real_b = B - eod before seqlens lists are sliced to real_b.
  seqlens_list_q:
    role: api_arg
    uo_id: actualSeqQlen
    encoding: normalize(seqlens_list_q) becomes actual_seq_qlen; TND layout branch only.
domains:
  D:
    operator: Dim=d
    compare: match
  prefix:
    operator: Name=prefix
    compare: tighter_profile
  inner_drop:
    operator: Name=dropMaskOuter
    compare: mismatch
  eod:
    operator: Dim=IsTnd
    compare: tighter_operator
  seqlens_list_q:
    operator: Name=actualSeqQlen
    compare: match
findings:
  - scale_value=1.0/math.sqrt(D) wires D into the call via a formula; no CSV scale_value or scale column (test_harness_gap for the literal 1.0 side).
  - inner_drop builds drop_mask locally; drop_mask does not appear in the richest call — feature role frozen, dropMaskOuter uo_id is adjacent tiling field not a call arg.
  - eod and seqlens_list_q logic runs only under TND layout (IsTnd); seqlens_list_kv also feeds the call but is outside this five-column slice.
  - prefix column is still read and passed despite profile empty_rate=1.0.
verify: Re-open the call window — D, prefix, seqlens_list_q must be api_arg with uo_id; inner_drop and eod feature; every non-null call_args.source_column is api_arg and listed above.
```
