```yaml
plan_tools:
  - Read entry script richest call window (precision entry, not profiler)
  - Read get_case / CSV-to-tensor mapping segment only
  - Read tiling_data.h once for short-name universe
  - uo-query only for header/call gaps (≤8 budget; do not spend all then leave PARTIAL)
  - Edit parts/bind.yaml semantic slots (mapping + domains)
  - pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path>
mapping:
  D:
    role: api_arg
    uo_id: d
    encoding: last dimension when building q/k/v tensors; same column is D inside scale_value=1/sqrt(D)
  prefix:
    role: api_arg
    uo_id: prefix
    encoding: CSV cell passed directly as prefix= kwarg (profile empty_rate=1.0; column still read)
  inner_drop:
    role: feature
    uo_id: dropMaskOuter
    encoding: when set, script builds drop_mask tensor; drop_mask is not passed in the richest call
  eod:
    role: feature
    uo_id: ""
    encoding: under TND layout only, real_b=B-eod then seqlens lists sliced to real_b before normalize; eod not in call
  seqlens_list_q:
    role: api_arg
    uo_id: actualSeqQlen
    encoding: under TND layout only, normalized and passed as actual_seq_qlen=
domains:
  D:
    operator: d
    compare: match
  prefix:
    operator: prefix
    compare: match
  inner_drop:
    operator: ""
    compare: ""
  eod:
    operator: ""
    compare: ""
  seqlens_list_q:
    operator: actualSeqQlen
    compare: match
findings:
  - scale_value=1/sqrt(D) is computed on-site; no CSV column named scale_value or scale (test_harness_gap)
  - inner_drop builds drop_mask but richest call omits drop_mask kwarg
  - eod: no matching tiling_data short name for end-of-document batch trim → PARTIAL uo_id left empty
  - seqlens_list_q and eod wiring active only under TND layout branch
verify: pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path> returns ok
```
