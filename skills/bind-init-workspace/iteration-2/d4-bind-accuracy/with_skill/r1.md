```yaml
plan_tools:
  - Open richest call window in precision entry script (not profiler)
  - Open get_case / CSV-to-variable mapping segment only
  - Read tiling_data header once for this operator's short-name set
  - Edit parts/bind.yaml semantic slots (mapping + domains)
  - pilot_cli inspect yaml --rel <draft relative to .ascendc-pilot>
mapping:
  D:
    role: api_arg
    uo_id: d
    encoding: Last dim when building q/k/v tensors; also the Python name in math.sqrt(D) for scale_value
  prefix:
    role: api_arg
    uo_id: prefix
    encoding: CSV cell passed directly as prefix= kwarg
  inner_drop:
    role: feature
    uo_id: dropMaskOuter
    encoding: When set, script builds a drop_mask tensor that is not passed into the call
  eod:
    role: feature
    uo_id: ""
    encoding: real_b = B - eod; seqlens lists sliced to real_b before normalize; TND layout branch only
  seqlens_list_q:
    role: api_arg
    uo_id: actualSeqQlen
    encoding: Input to normalize(...) that becomes actual_seq_qlen kwarg; TND layout branch only
domains:
  D:
    operator: d
    compare: match
  prefix:
    operator: prefix
    compare: match
  inner_drop:
    operator: ""
    compare: ""
  eod:
    operator: ""
    compare: ""
  seqlens_list_q:
    operator: actualSeqQlen
    compare: match
findings:
  - "PARTIAL: eod is feature but no matching switch/field in tiling_data header; uo_id left empty"
  - "test_harness_gap: scale_value=1.0/math.sqrt(D) is computed at call site; no scale_value or scale CSV column"
  - "inner_drop builds drop_mask tensor that does not appear in npu_fusion_attention_grad_v2 call"
  - "prefix profile empty_rate=1.0 but column is still read and bound as api_arg"
  - "eod and seqlens_list_q mapping branch runs only under TND layout"
verify: pilot_cli inspect yaml --rel <draft relative to .ascendc-pilot> returns ok
```
