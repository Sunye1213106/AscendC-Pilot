# iteration-5 live bind0 re-eval

Bar: Composer 2.5 fills `parts/bind0.yaml` using the production `bind-init` columns playbook, real `uo-query` (one no-arg + per-active identifier, no Dim catalog dump), wall clock under 4 minutes, gold keys in `pr_workspace/bind_eval/grade_bind0.py`.

Live run files were not wiped. Baseline bind0 copied to `pr_workspace/bind_eval/baseline/`. Session `method_bind.md` + `refs/bind/column-binding-edge-cases.md` refreshed from production skill so the subagent sees the same injection Host would ship.
