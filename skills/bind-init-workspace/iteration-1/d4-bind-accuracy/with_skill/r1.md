```yaml
plan_tools:
  - Read repo_scan.yaml tables[].profile for the five column value ranges
  - Open richest call torch_npu.npu_fusion_attention_grad_v2 plus get_case prefix/seqlens/eod/inner_drop mapping window
  - Read flash_attention_score_grad_tiling_data_regbase.h once; copy matching short names into uo_id
  - One uo-query for dim_names/hints only if a header+call gap remains (not eight per-column queries)
  - Edit parts/bind.yaml call_args, mapping, and domains for these five columns
  - pilot_cli inspect yaml --rel actions/bind_init/parts/bind.yaml under the arch35 run .ascendc-pilot root
mapping:
  D: {role: api_arg, uo_id: d, encoding: integer Q/K/V head dimension for tensor shapes; also feeds live scale_value=1/sqrt(D) but is not scale_value source_column}
  prefix: {role: api_arg, uo_id: prefix, encoding: optional prefix list/tensor passed as prefix= kwarg; column always read though empty_rate=1.0 in this CSV}
  inner_drop: {role: feature, uo_id: dropMaskOuter, encoding: 0/1 flag gating inner dropout drop_mask tensor generation in Python; drop_mask is not a torch_npu call kwarg}
  eod: {role: feature, uo_id: , encoding: TND-only trailing empty-batch count; sets real_b=B-eod and trims seqlens before normalize; eod is not a call kwarg}
  seqlens_list_q: {role: api_arg, uo_id: actualSeqQlen, encoding: TND list-literal per-batch query lengths; normalized into actual_seq_qlen call kwarg}
domains:
  D: {operator: d, compare: match}
  prefix: {operator: prefix, compare: match}
  inner_drop: {operator: dropMaskOuter, compare: match}
  eod: {operator: , compare: }
  seqlens_list_q: {operator: actualSeqQlen, compare: match}
findings:
  - scale_value=1/sqrt(D) is a live formula with source_column null; D binds tiling dim d not scaleValue
  - prefix remains api_arg despite empty_rate=1.0; runner still reads the column and passes prefix=
  - seqlens_list_q is TND-path only in this script; uo_id/operator are actualSeqQlen proto short name not Dim=IsTnd
  - inner_drop is feature with header dropMaskOuter; gates drop_mask generation outside the richest call
  - eod PARTIAL — TND trim feature with no dedicated header or call slot; no uo_id (would collide with B→b if forced)
  - header-first binding covered d/prefix/dropMaskOuter/actualSeqQlen without spending the full eight identifier-query budget
verify: pilot_cli inspect yaml --rel actions/bind_init/parts/bind.yaml relative to the arch35 .ascendc-pilot run directory before handoff
```
