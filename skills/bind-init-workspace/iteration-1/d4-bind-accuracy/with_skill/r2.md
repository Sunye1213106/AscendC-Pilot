```yaml
plan_tools:
  - Open richest call torch_npu.npu_fusion_attention_grad_v2 + get_case mapping; write call_args before roles
  - Read flash_attention_score_grad_tiling_data_regbase.h once; copy short names (d, prefix, actualSeqQlen, dropMaskOuter, …)
  - One no-param uo-query for dim_names / hint only
  - Identifier queries only for header/call gaps (≤8 total; do not burn budget then leave columns empty)
  - Edit parts/bind.yaml mapping + domains from frozen roles
  - pilot_cli inspect yaml --rel parts/bind.yaml before handoff
mapping:
  D: {role: api_arg, uo_id: d, encoding: CSV head dim for q/k/v tensor shapes; feeds scale_value=1/sqrt(D) formula but scale_value source_column is null}
  prefix: {role: api_arg, uo_id: prefix, encoding: get_case reads prefix column; passed as prefix= kwarg even when profile empty_rate=1.0}
  inner_drop: {role: feature, uo_id: dropMaskOuter, encoding: CSV 0/1 flag generates drop_mask tensor in Python; drop_mask is not a grad-call kwarg}
  eod: {role: feature, uo_id: actualSeqQlen, encoding: TND-only trim; real_b=B-eod shortens seqlens lists; eod itself is not a call kwarg}
  seqlens_list_q: {role: api_arg, uo_id: actualSeqQlen, encoding: normalize(seqlens_list_q) feeds actual_seq_qlen kwarg; TND-only in this script}
domains:
  D: {operator: d, compare: match}
  prefix: {operator: prefix, compare: match}
  inner_drop: {operator: "", compare: ""}
  eod: {operator: "", compare: ""}
  seqlens_list_q: {operator: actualSeqQlen, compare: match}
findings:
  - scale_value=1/sqrt(D) is a live formula; scale_value source_column null; D binds tiling short dim d not scaleValue
  - prefix remains api_arg per richest call despite empty_rate=1.0
  - inner_drop is feature with dropMaskOuter from header; drop_mask tensor is not a call kwarg
  - eod is TND-only feature trim (real_b, seqlens); no direct tiling_data field → uo_id actualSeqQlen is best-effort PARTIAL
  - seqlens_list_q operator/uo_id is actualSeqQlen proto name, not layout switch Dim=IsTnd
verify: pilot_cli inspect yaml --rel parts/bind.yaml returns ok; checklist—api_arg uo_id filled, D→d not scaleValue, inner_drop feature+dropMaskOuter, eod feature, seqlens not Dim=IsTnd
```
