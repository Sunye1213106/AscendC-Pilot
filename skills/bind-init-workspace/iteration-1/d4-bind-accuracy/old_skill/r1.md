```yaml
plan_tools:
  - Open repo_scan.yaml + precision entry script richest call (torch_npu.npu_fusion_attention_grad_v2) and get_case mapping
  - Read flash_attention_score_grad_tiling_data_regbase.h once; copy short tiling field names into uo_id
  - Write call_args from richest-call kwargs before assigning any column role
  - uo-query --project <op> (no-arg index) once for dim_names/hint; only query identifiers missing from header
  - Edit parts/bind.yaml mapping + domains semantic slots (no Write full-file rewrite)
  - pilot_cli inspect yaml --rel parts/bind.yaml to confirm schema before handoff
mapping:
  D: {role: api_arg, uo_id: d, encoding: head dim for q/k/v tensor shapes; scale_value=1/sqrt(D) is a live formula with source_column null}
  prefix: {role: api_arg, uo_id: prefix, encoding: List[int] prefix kwarg; CSV empty_rate=1.0 but column still read and passed}
  inner_drop: {role: feature, uo_id: dropMaskOuter, encoding: 0/1 gates Python drop_mask tensor generation; drop_mask is not a grad-v2 kwarg}
  eod: {role: feature, uo_id: '', encoding: TND-only tail-padding count; real_b=B-eod and seqlens trim, eod not a call kwarg}
  seqlens_list_q: {role: api_arg, uo_id: actualSeqQlen, encoding: TND segment-length list normalized to actual_seq_qlen prefix-sum kwarg}
domains:
  D: {operator: d, compare: match}
  prefix: {operator: prefix, compare: match}
  inner_drop: {operator: '', compare: ''}
  eod: {operator: '', compare: ''}
  seqlens_list_q: {operator: actualSeqQlen, compare: match}
findings:
  - scale_value kwarg is 1/sqrt(D) with source_column null; D binds tiling short dim d, not scaleValue
  - prefix is api_arg despite empty_rate=1.0; runner may synthesize values when CSV cell empty
  - inner_drop is feature (drop_mask builder), not api_arg; uo_id from header dropMaskOuter
  - eod is feature (batch/seqlens trim for TND), not api_arg; no dedicated tiling header field (PARTIAL uo_id)
  - seqlens_list_q feeds actual_seq_qlen kwarg; operator is actualSeqQlen, not Dim=IsTnd
verify: pilot_cli inspect yaml --rel parts/bind.yaml returns ok against the bind draft parser
```
