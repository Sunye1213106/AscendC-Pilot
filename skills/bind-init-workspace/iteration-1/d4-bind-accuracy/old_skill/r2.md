```yaml
plan_tools:
  - Open richest call torch_npu.npu_fusion_attention_grad_v2; write call_args before any role
  - Read flash_attention_score_grad_tiling_data_regbase.h once; copy matching short names (d, prefix, actualSeqQlen, dropMaskOuter, …)
  - One no-param uo-query for dim_names / hint only
  - Identifier queries only for header/call gaps (not 8 blind passes)
  - Fill domains.profile from tables[].profile; operator = uo_id short name (not Dim=IsTnd for seqlens)
  - pilot_cli inspect yaml --rel parts/bind.yaml before handoff
mapping:
  D: {role: api_arg, uo_id: d, encoding: CSV head dim for q/k/v tensors; also used in 1/sqrt(D) but scale_value source_column is null}
  prefix: {role: api_arg, uo_id: prefix, encoding: get_case reads prefix; passed as prefix= kwarg even when profile empty_rate=1.0}
  inner_drop: {role: feature, uo_id: dropMaskOuter, encoding: CSV flag builds drop_mask tensor in Python; drop_mask is not a call kwarg}
  eod: {role: feature, uo_id: "", encoding: TND-only; real_b=B-eod trims seqlens lists; eod is not a call kwarg}
  seqlens_list_q: {role: api_arg, uo_id: actualSeqQlen, encoding: normalize(seqlens_list_q) → actual_seq_qlen kwarg; TND-only in this script}
domains:
  D: {operator: d, compare: match}
  prefix: {operator: prefix, compare: match}
  inner_drop: {operator: dropMaskOuter, compare: match}
  eod: {operator: "", compare: ""}
  seqlens_list_q: {operator: actualSeqQlen, compare: match}
findings:
  - scale_value=1/sqrt(D) is a live formula; no CSV scale column; D binds tiling d not scaleValue
  - prefix column read despite empty_rate=1.0; remains api_arg per richest call
  - seqlens_list_q and eod are TND-path only; eod has no header short name → uo_id PARTIAL in findings only
  - inner_drop is feature with dropMaskOuter from header; do not bind drop_mask as api_arg
verify: pilot_cli inspect yaml --rel parts/bind.yaml returns ok; checklist—api_arg columns have uo_id, D not scaleValue, seqlens not Dim=IsTnd
```
