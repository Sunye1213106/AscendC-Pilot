```yaml
plan_tools:
  - Read repo_scan.yaml table headers and tables[].profile
  - Open precision entry script; locate richest torch_npu.npu_block_matmul call (not profiler)
  - Write call_args from call-site kwargs/positionals before assigning any column role
  - uo-query (no args) for dim_names Packed and DType
  - uo-query identifiers b, width, packedSeg, maskOuter within budget
  - Edit parts/bind.yaml semantic slots only (mapping, domains, findings)

mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: Last dim when building left/right; also the Python name inside math.sqrt(...) for scale=.
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed directly as batch= kwarg.
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: Segment list normalized to packed=; branch runs only under Packed layout.
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: Tail pad count; real_n=Batch-pad_tail slices segs before normalize; not in call.
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: Flag to build outer mask in Python pre-step; mask is not a call kwarg.

domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: "Dim=Packed, packedSeg"
    compare: match
  pad_tail:
    operator: padToken
    compare: tighter_operator
  gate_on:
    operator: maskOuter
    compare: match

findings:
  - scale=1.0/math.sqrt(Width) uses Width but no CSV column named scale; source_column null gap.
  - mask tensor is built when gate_on is set but mask is not a kwarg of npu_block_matmul.
  - pad_tail only adjusts segs slice in Python preprocessing; column remains feature, not api_arg.
  - Header fields width1 and scaleValue have no CSV columns; scaleValue is derived from Width at call site.
  - pad_tail uo_id bound to padToken as nearest header field; semantic is pad count not token id (PARTIAL).

verify: Cross-check call_args against the richest call—every non-null source_column is api_arg with uo_id, preprocessing-only columns are feature without false api_arg, and scale/mask gaps are recorded in findings.
```
