plan_tools:
  - Read richest precision-entry call window (torch_npu.npu_block_matmul) and case-builder mapping segment
  - Read repo_scan.yaml tables[].profile for domain profiles
  - Read tiling_data .h header once for short-name set
  - Edit parts/bind.yaml mapping and domains per frozen roles
  - uo-query only for ≤8 identifier gaps not resolved by header or call names
  - pilot_cli inspect yaml --rel parts/bind.yaml
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: CSV last-axis size for left/right tensors; also the Width inside math.sqrt for scale=
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed directly as batch=
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: segment lengths normalized then passed as packed=; branch runs only under Packed layout
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: real_n = Batch - pad_tail; segs sliced to real_n before normalize; not in call kwargs
  gate_on:
    role: feature
    uo_id:
    encoding: when set, Python builds a mask tensor for pre-step only; mask is not a call kwarg
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator:
    compare:
  gate_on:
    operator:
    compare:
findings:
  - scale= is computed as 1.0/math.sqrt(Width); no CSV column named scale (source_column null for that kwarg)
  - mask built when gate_on is set but mask does not appear in npu_block_matmul kwargs
  - segs/packed wiring applies only under Packed layout (dim_names Packed)
  - PARTIAL gate_on — header maskOuter names mask geometry, not the enable flag; uo_id left empty
verify: pilot_cli inspect yaml --rel parts/bind.yaml
