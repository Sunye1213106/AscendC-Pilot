```yaml
plan_tools:
  - Read repo_scan.yaml table headers and tables[].profile
  - Open richest precision-entry call window and case-builder mapping segment
  - Read tiling_data header once for this operator's short-name set
  - Edit parts/bind.yaml call_args then mapping roles and uo_id
  - uo-query only for header/call gaps (≤8 budget; do not pre-spend then PARTIAL)
  - pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path>
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: CSV last-dimension size for left/right tensors; also the Width inside math.sqrt for scale
  Batch:
    role: api_arg
    uo_id: b
    encoding: batch dimension passed directly as batch= kwarg
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: segment descriptor column normalized and passed as packed= (Packed layout branch)
  pad_tail:
    role: feature
    uo_id: ''
    encoding: tail padding count subtracted from Batch before slicing segs; not a call kwarg
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: flag enabling outer-mask tensor build in Python pre-step; mask is not a call kwarg
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator: ''
    compare: ''
  gate_on:
    operator: ''
    compare: ''
findings:
  - scale=1.0/math.sqrt(Width) is computed at call site; no CSV column named scale (test_harness_gap for scale kwarg)
  - PARTIAL pad_tail uo_id: header padToken is padding token id, not tail-padding count; leave empty, do not borrow padToken
verify: pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path> returns ok
```
