```yaml
plan_tools:
  - Read richest precision-entry call window (torch_npu.npu_block_matmul site)
  - Read case-builder mapping segment (column → tensor / kwarg wiring)
  - Read tiling_data.h once for this operator's short-name universe
  - Draft call_args then freeze mapping roles before any uo_id assignment
  - Edit parts/bind.yaml semantic grid (mapping + domains only)
  - pilot_cli inspect yaml --rel <draft relative to .ascendc-pilot>
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: CSV integer; last-axis extent when building left/right and divisor inside math.sqrt for scale=
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV integer passed verbatim as batch= kwarg
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: CSV segment descriptor; normalize(segs) feeds packed= (Packed layout branch only)
  pad_tail:
    role: feature
    uo_id: padToken
    encoding: CSV integer; real_n = Batch - pad_tail slices segs before normalize — not in call
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: CSV enable flag; when set, runner builds mask tensor in Python pre-step only — mask not a call kwarg
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator:
    compare:
  gate_on:
    operator:
    compare:
findings:
  - "scale=1.0/math.sqrt(Width) is computed at call site; no CSV column named scale (source_column null for scale kwarg)."
  - "gate_on drives a Python-only mask pre-step; mask does not appear in npu_block_matmul kwargs."
  - "segs→packed wiring runs only under Packed layout (dim_names); column remains api_arg per richest call."
verify: pilot_cli inspect yaml --rel iteration-3/alt-scene-bind/with_skill/bind.yaml
```
