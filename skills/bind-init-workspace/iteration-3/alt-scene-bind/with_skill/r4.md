```yaml
plan_tools:
  - Open richest precision-entry call window and case-builder mapping segment (no full runner read)
  - Write call_args from torch_npu.npu_block_matmul kwargs before assigning roles
  - Trace each column to call/site usage; freeze roles (size vs rewrite vs meta)
  - Read tiling_data header once; map uo_id by kwargs source_column then field semantics
  - Edit parts/bind.yaml semantic slots only
  - pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path>
mapping:
  Width:
    role: api_arg
    uo_id: width
    encoding: last dim when building left/right; also the value inside math.sqrt(...) for scale kwarg
  Batch:
    role: api_arg
    uo_id: b
    encoding: CSV cell passed directly as batch=
  segs:
    role: api_arg
    uo_id: packedSeg
    encoding: segment descriptor fed to normalize(...) for packed=; branch active under Packed layout
  pad_tail:
    role: feature
    uo_id: ""
    encoding: subtracted from Batch to compute real_n before slicing segs; not in call
  gate_on:
    role: feature
    uo_id: maskOuter
    encoding: when set, builds outer mask tensor in Python pre-step only; mask not passed to call
domains:
  Width:
    operator: width
    compare: match
  Batch:
    operator: b
    compare: match
  segs:
    operator: packedSeg
    compare: match
  pad_tail:
    operator: ""
    compare: ""
  gate_on:
    operator: ""
    compare: ""
findings:
  - scale=1.0/math.sqrt(Width) is a computed kwarg; no CSV column named scale (source_column null)
  - pad_tail rewrites segs via Batch-pad_tail slice; padToken is padding token id (different quantity) — uo_id PARTIAL, left empty
  - segs remains api_arg even though used only under Packed layout; operator is packedSeg not Dim=Packed
verify: pilot_cli inspect yaml --rel <draft relative .ascendc-pilot path>
```
