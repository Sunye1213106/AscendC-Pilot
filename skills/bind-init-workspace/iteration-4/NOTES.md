# iteration-4 live FAG bind eval

Bar: Composer 2.5 independently fills the real `parts/bind.yaml` for bind-init AXIS=bind (subagent2 of d4), using real `uo-query`, zero binding errors, wall clock under 5 minutes.

Gold keys live only in `grade_live_bind.py`. Production `skills/bind-init/` must not name this operator's fields.

Session method/refs were replaced with current skill copies. Historical fail YAML is in `d4-fail/`.
