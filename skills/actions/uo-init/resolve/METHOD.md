# resolve

Resolve only unresolved semantic gaps. Producer writes staging parts; merge via `apply_gap_patch`.

Agent: `uo-semantic-resolver`. Prompt: `uo/resolve-gaps`.
Do not restate domain invariants here.
