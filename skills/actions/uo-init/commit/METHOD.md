# commit

Assemble legacy KB layers as needed, then compile the unified CodeMap into:

```text
.ascendc-pilot/uo/<op>.<arch>.uo
```

Also refreshes integrity / adapter pack for TG transition. YAML projections are dump-only.
