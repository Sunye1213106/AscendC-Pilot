# 写计划易错点

**何时加载**：写 `ce/plan/{slug}_plan.md` 时。

- 正式产物是这份 markdown：实现分析、todo、测试内容。禁止写任何 CE yaml。
- 语义走查询，形态见 code-access 不变量。
- todo 必须是可勾选的 `- [ ]`，后续 apply 只吃未完成项。
- 「测试内容」给 TG 读，不要在本步发明 cases 表。
