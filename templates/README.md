# Host adapter templates

Install scripts stamp `install_stamp.txt` under `templates/<platform>/`.

Skill sources live in repo-root `skills/` — one source, three hosts.
Host differences are limited to PLUGIN_ROOT paths in PATHS / runtime docs.
