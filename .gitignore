.claude/
.opencode/
.tmp-cbm-install/
# Local-only eval oracles (do not commit)
.tmp-oracle/

# Cached clang bundles / derivation runs for scripts/_probe_derive.py
.probe_cache/

# Python cache and test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
# pytest --basetemp dirs. These held full UO run products (YAML, SQLite), which
# file discovery and scope closure would otherwise pick up as if they were source.
.pytest-tmp/
.pytest_tmp/
# One-off local probes. Nothing here is a reusable entry point.
.pytmp/

# Packaging/build metadata
*.egg-info/
build/
dist/

# Local environments
.venv/
venv/
env/

# Operator-local agent products
.ascendc-agent/
.understand-operator/
.testcase-generator/

# Editor/system files
.idea/
.vscode/
.DS_Store
Thumbs.db

# Agent/session transcripts (never commit)
session-ses_*.md
session-*.md

