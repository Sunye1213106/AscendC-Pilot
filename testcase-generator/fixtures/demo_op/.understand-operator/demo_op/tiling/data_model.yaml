version: 1
op_name: demo_op
structs:
  BaseParams:
    present_when: {IsTnd: 0}
    fields: [s1BaseSize, s2BaseSize]
  TndParam:
    present_when: {IsTnd: 1}
    fields: [actualSeqQLen]
numeric_overlay:
  has_varlen:
    shared_key_with: TF002
    differs_in: [TndParam]
