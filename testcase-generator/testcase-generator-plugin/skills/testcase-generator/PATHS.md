# testcase-generator 脚本路径

```text
PLUGIN_ROOT = testcase-generator-plugin/
PACKAGE     = testcase_generator/scripts/
SKILL_DIR   = skills/testcase-generator/   # wrapper scripts for install
```

OpenCode/Cursor 安装后使用 junction 指向本目录；也可 `pip install -e .` 后直接用 `tg-init` 等命令。
