# Repair Diagnosis

After 3 repair rounds with persistent gaps, LLM may diagnose:

- input_realization wrong
- rule missing
- KB wrong
- hidden source constraint
- obligation actually unreachable

Output: suggestion only in `review/` or chat. Do not modify `coverage_audit.yaml` directly.
