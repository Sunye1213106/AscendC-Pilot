# Domain contracts

人类溯源索引：某个跨 Skill 概念的 **owner Skill 是谁**。不参与 runtime 选择。

运行时只认当前 Action 显式声明的 `method_ref` / `refs` / `knowledge_refs`。不要链到别人的 `references/`。

跨 Skill 需要复用方法时，指针到 `skills/<id>/SKILL.md`，或升级为 Policy / CONTEXT。
