# Domain contract: test-script repo

Owner: `bind-init` (`skills/bind-init/references/harness.md`).

Consumers: CE PRs that patch operator test scripts.
The TG init product `tg/init/test_repo_contract.yaml` is the runtime interface.
Do not inline the owner skill from a foreign SKILL body.
