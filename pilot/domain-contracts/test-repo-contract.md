# Domain contract: test-script repo

Owner: `testcase-generation` (`skills/testcase-generation/references/test-script-repo.md`).

Consumers: `code-engineering` CE PRs that patch operator test scripts.
The TG init product `tg/init/test_repo_contract.yaml` is the runtime interface.
Do not inline the owner skill from code-engineering METHOD / SKILL body.
