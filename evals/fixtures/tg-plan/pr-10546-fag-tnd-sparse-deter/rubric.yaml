schema: tg-plan-rubric/v1
case: pr-10546-fag-tnd-sparse-deter
product: tg/plan.md
operator: FlashAttentionScoreGrad
architecture: arch35

notes: |
  Semantic grading for PR 10546 (TND g==1 sparse_mode 2/3/4 deter path).
  Pass = unique-write + HIT-arm classification + Solve-contract.
  Scale (R8) is informational. IDs and exact seq/N values are free.

new_or_rewritten_fields:
  - deterMaxRound
  - deterPrefix0
  - deterPrefix1
  - deterPrefix2

probeable_names:
  - rLine
  - rPool
  - rSingle
  - leftUp
  - isBand
  - isSmall
  - useLine
  - runHasSingle
  - pairRounds
  - shapeKind

confirmed_columns:
  - Input_Layout
  - B
  - N1
  - N2
  - S1
  - S2
  - D
  - D_V
  - Drop_Out_Possibility
  - Pre_Tockens
  - Next_Tockens
  - sparse_mode
  - PSE_type
  - seqlens_list_q
  - seqlens_list_kv
  - eod
  - same_as_input
  - seed
  - offset
  - is_deter
  - rope
  - inner_drop
  - prefix

unresolved_must_gap:
  - Dtype
  - Atten_mask_shape

scoring:
  required_ids: [R1, R2, R3, R4, R5, R6, R7, R9, R10, R11, R12, R13]
  informational_ids: [R8]
  min_dimensions: 4
  min_partitions: 8
  min_guards: 3
  require_l1: true
  budget_seconds: 300
  budget_required: true
