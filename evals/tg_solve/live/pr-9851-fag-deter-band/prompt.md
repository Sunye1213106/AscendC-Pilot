<task>
为 OPEN 义务交回 `schema: tg-solve-fill/v1`。不要手写义务条数，不要枚举行，不要宣布 HIT。
</task>

<input>
- Plan: `D:/PR-review/AscendC-Pilot/evals/fixtures/tg-plan/pr-9851-fag-deter-band/session/trial-c25-fill1.yaml`
- Init: `D:/PR-review/TEST/.ascendc-pr/gitcode.com--cann--ops-transformer--pr-9851/attention/flash_attention_score_grad/.ascendc-pilot/arch35/tg/init.yaml`
- Solve index: `D:/PR-review/AscendC-Pilot/evals/tg_solve/live/pr-9851-fag-deter-band/solve_index.yaml`
- project_root: `D:/PR-review/TEST/.ascendc-pr/gitcode.com--cann--ops-transformer--pr-9851/attention/flash_attention_score_grad`
</input>

<method>
先读本目录 `method.md`，那就是本窗形式规范。禁止打开 `evals/fixtures` 下除 Plan 输入路径以外的文件。禁止读 rubric / grade / trial。
只填 `baseline` / `hits` / 可选 `guard_witnesses`。不要写 `columns` / `rows` / `unreachable`。
</method>

<output>
最终消息正文就是 `schema: tg-solve-fill/v1` YAML 全文。不要 Write。
</output>
