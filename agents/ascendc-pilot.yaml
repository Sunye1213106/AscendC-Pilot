id: ascendc-pilot
version: 1
name_zh: AscendC-Pilot
role: producer
mode: primary
description: >-
  AscendC-Pilot primary control agent. Load workflow skills by description
  (uo-init / tg-init / …) then acp start; Build/Plan tabs bypass harness.
read_scopes:
  - "*"
write_scopes:
  []
forbidden:
  - modify_pilot_state
  - declare_workflow_passed
  - write_outside_declared_scope
tool_budget:
  max_calls: 12
