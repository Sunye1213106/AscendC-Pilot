---
name: tg-init
description: >-
  DEPRECATED. Intake is included in tg-plan. Prefer /tg-plan.
argument-hint: "<project_root> --op-name <op_name>"
---

# /tg-init (deprecated)

Use `/tg-plan` instead. Intake + extract + plan are one command.

See `skills/PATHS.md` for OpenCode install locations.

Legacy wrapper still runs intake-only if needed:

```powershell
tg-init <project_root> --op-name <op_name>
```
