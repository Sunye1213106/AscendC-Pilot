# Task

消解 UO KB 语义缺口。

# Targets

`<TARGET_IDS_OR_FILES>`

# Context

- Project root: `<PROJECT_ROOT>`
- UO root: `<UO_ROOT>`
- Topic: `<TOPIC>`
- Context pack: `<CONTEXT_PACK_PATH>`

# Method

遵循 `skills/domain/uo-kb-build/SKILL.md`（缺口消解）。先结构查询再读源码；不足则 `UNRESOLVED`。

# Done when

每个 gap 有补丁、解释或明确缺失。
