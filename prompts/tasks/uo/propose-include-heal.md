<task>
对照 include-heal unresolved 与真实 CANN/ops 树，写出建议的额外 -I 目录。
</task>

<context>
- Unresolved 清单与 `environment_capabilities.yaml` 的 cann.root
- 方法细节见 session method（`propose-include-heal`）
</context>

<output>
只写本步 `propose_include_heal` 草稿 YAML。
不写 extras、不写 `spec/build_context.yaml`、不改算子源码。
</output>
