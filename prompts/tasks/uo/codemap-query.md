<task>
回答用户对已有 AscendC Operator CodeMap（`.uo`）的问题。只读，不改正式 CodeMap。
</task>

<input>
用户问题见 stub「USER QUESTION」。
用 `pilot_cli` command=`uo-query --project <算子绝对路径>` 查图。Host cwd 是 Pilot 仓。
</input>

<output>
完整自然语言答案。不写文件，不推进工作流。
</output>
